# API Keys (at least one is required)
OPENAI_API_KEY=your-openai-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: Skip vectorstore building on startup (useful if API quota is limited)
# The vectorstore will be built on first query instead
LAZY_LOAD=true